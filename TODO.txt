- replace getters by direct field access (which can in turn be backed by a function)
- add test case where variables have no depth dimension
- implement classes Matchup + MatchupCreator
- add logging facility
- extend interface of DataStorage by methods that manipulate data
